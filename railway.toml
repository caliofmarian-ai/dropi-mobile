[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm build"

[deploy]
startCommand = "node dist/migrate.mjs && node dist/validate-db.mjs && pnpm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[healthcheck]
path = "/api/health"
timeout = 30
